═══════════════════════════════════════════════════════════════
    COMANDOS ÚTEIS - GEOAPP
═══════════════════════════════════════════════════════════════

📦 CONFIGURAÇÃO INICIAL
───────────────────────────────────────────────────────────────
composer install                           # Instalar dependências
copy .env.example .env                     # Copiar configuração
php artisan key:generate                   # Gerar chave da aplicação
php artisan storage:link                   # Link simbólico storage

🐳 DOCKER
───────────────────────────────────────────────────────────────
docker-compose up -d                       # Iniciar containers
docker-compose down                        # Parar containers
docker-compose logs -f                     # Ver logs
docker-compose ps                          # Status dos containers

🗄️ BANCO DE DADOS
───────────────────────────────────────────────────────────────
php artisan migrate                        # Executar migrações
php artisan migrate:fresh                   # Recriar banco do zero
php artisan migrate:rollback               # Reverter última migração
php artisan migrate:status                 # Status das migrações

👤 USUÁRIOS
───────────────────────────────────────────────────────────────
php artisan make:filament-user             # Criar usuário admin

🔧 DESENVOLVIMENTO
───────────────────────────────────────────────────────────────
php artisan serve                          # Iniciar servidor dev
php artisan clear-cache                    # Limpar cache
php artisan config:clear                   # Limpar config cache
php artisan route:clear                    # Limpar route cache
php artisan view:clear                     # Limpar view cache
php artisan optimize                       # Otimizar aplicação

🐛 DEBUG
───────────────────────────────────────────────────────────────
php artisan tinker                         # Tinker (console interativo)
tail -f storage/logs/laravel.log           # Ver logs em tempo real

🧪 TESTES
───────────────────────────────────────────────────────────────
php artisan test                           # Rodar testes
php artisan test --parallel                # Testes em paralelo

📊 INFO
───────────────────────────────────────────────────────────────
php artisan route:list                     # Listar todas as rotas
php artisan env                            # Ver variáveis de ambiente
php artisan about                          # Info sobre Laravel

🧹 LIMPEZA
───────────────────────────────────────────────────────────────
composer dump-autoload                     # Recriar autoload
php artisan optimize:clear                 # Limpar todas otimizações

═══════════════════════════════════════════════════════════════
    ACESSOS
═══════════════════════════════════════════════════════════════

🌐 Aplicação
───────────────────────────────────────────────────────────────
Mapa:        http://localhost:8000
Admin:       http://localhost:8000/painel
API:         http://localhost:8000/api/camadas

🗄️ PostgreSQL (Docker)
───────────────────────────────────────────────────────────────
Host:        localhost
Porta:       5432
Database:    geoapp_db
Usuário:     geoapp_user
Senha:       geoapp_password

═══════════════════════════════════════════════════════════════

💡 DICAS
───────────────────────────────────────────────────────────────
• Sempre use PHP 8.4+
• Configure .env antes de rodar migrations
• Primeiro crie usuário admin antes de acessar /painel
• Use docker-compose para ambiente isolado
• Veja INSTALACAO.md para guia completo

═══════════════════════════════════════════════════════════════






